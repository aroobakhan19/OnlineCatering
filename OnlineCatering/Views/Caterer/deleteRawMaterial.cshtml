@model OnlineCatering.Models.RawMaterial

@{
    ViewData["Title"] = "deleteRawMaterial";
}

<h1>deleteRawMaterial</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RawMaterial</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    
    <form asp-action="deleteRawMaterial">
        <input type="hidden" asp-for="IngredientNo" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="RawMaterials">Back to List</a>
    </form>
</div>
